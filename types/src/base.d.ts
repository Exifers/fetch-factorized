import { config, Object, fetchBaseType } from './types';
export declare const fetchBaseBody: (url: any, body: any, context?: {}, config?: config) => Promise<Object>;
export declare const fetchBase: fetchBaseType;
